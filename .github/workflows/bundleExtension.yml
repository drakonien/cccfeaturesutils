# This workflow will do a clean install of node dependencies, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: VSCode Extension Bundle
on:
  workflow_dispatch:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [14.x]
    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - run: npm install
    - run: npm install -g vsce
    - run: vsce package
    - name: Inject slug/short variables # Add to the action variables with information as branch name
      uses: rlespinasse/github-slug-action@2.0.0
    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ env.GITHUB_REF_SLUG }}
        body_path: LATEST_CHANGELOG.md
        release_name: Release ${{ env.GITHUB_REF_SLUG }}
        draft: false
        prerelease: false
    - name: Upload Release Asset
      id: upload-release-asset 
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }} # This pulls from the CREATE RELEASE step above, referencing it's ID to get its outputs object, which include a `upload_url`.
        asset_path: /home/runner/work/cccfeaturesutils/cccfeaturesutils/cccfeaturesutils-${{ env.GITHUB_REF_SLUG }}.vsix
        asset_name: cccfeaturesutils-${{ env.GITHUB_REF_SLUG }}.vsix
        asset_content_type: application/zip
        
